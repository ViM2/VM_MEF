%% VMt
%% mots clefs castem: http://www-cast3m.cea.fr/html/utilitaires/cast3m.xml
%% et gibiane: http://www-cast3m.cea.fr/html/utilitaires/gedit/gibiane.lang
\RequirePackage{listings}
\RequirePackage{xcolor}

\lstdefinelanguage{ansys}{%[]{C++}{%
  alsoletter={*/},
  morekeywords=[2]{/nop,/sys,/gop,/PREP7,/STAT,/POST1,/SOLU,
    *get,*dim,*cfopen,*vwrite,*do,*enddo,*cfclose,*vread,*vput,*vget,*USE},
  morekeywords={nall,nsel,esel,lsel,asel,node,elem,mat,count,pldisp,
    ET,PLANE,UIMP,EX,NUXY,EMIS,k,larc,l,amesh,PLANE82,
    DL,dk,fx,fy,fz,ux,uy,uz,a,fk,symm,
    SOLU,SOLVE,FINISH,    
        % Fourth category of Keywords
    % Other Reserved Words
    x, y, z, pi, i,
    sin, cos, tan, atan, asin, acos,
    cotan,sinh,cosh,tanh,cotanh,
    exp, log, log10, sqrt
    abs, max, min
  },
  sensitive=true,
  morecomment=[l]{!},
  morestring=[b]",%
  morestring=[b]',%
}[keywords, comments, strings, directives]

